/** Standard Recharts margin — axis positioning, not layout direction. */
export const RECHARTS_MARGIN_DEFAULT = {
  top: 8,
  right: 8,
  left: 0,
  bottom: 0,
} as const;

export const RECHARTS_MARGIN_COMPACT = {
  top: 8,
  right: 4,
  left: 4,
  bottom: 0,
} as const;
