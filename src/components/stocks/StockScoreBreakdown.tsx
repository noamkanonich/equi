"use client";

import styled from "styled-components";

export function StockScoreBreakdown() {
  return <Wrapper>StockScoreBreakdown</Wrapper>;
}

const Wrapper = styled.section`
  padding: 1rem;
`;
